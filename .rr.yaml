version: '3'

logs:
  mode: production
  level: warn
  output: stderr
  encoding: console

http:
  middleware:
    - gzip
    - static
  static:
    dir: "public"
    forbid:
      - ".php"
      - ".htaccess"
